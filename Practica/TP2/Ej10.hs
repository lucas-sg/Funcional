-- No tiene tipo porque estoy diciendo que tom recibe una funcion x (porque 
-- a la segunda x le estoy aplicando x (la primera)). Esta funcion x recibe una
-- funcion, que recibe una funcion, que recibe una funcion... X tendria que
-- tener tipo infinito, no compila.

-- tom x = x x